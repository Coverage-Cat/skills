interface:
  display_name: "Coverage Cat Umbrella"
  short_description: "Run Coverage Cat's consumer-prefill or delegated umbrella flow"
  default_prompt: "Use $coverage-cat-umbrella-purchase to operate Coverage Cat's umbrella purchase workflow. Default to the consumer-prefill handoff when no operator bearer key is available in runtime, and only use the delegated flow when a real operator key is already present."
