interface:
  display_name: "Umbrella Insurance Compare And Purchase"
  short_description: "Run Coverage Cat's consumer-prefill or delegated umbrella compare-and-purchase flow"
  default_prompt: "Use $umbrella-insurance-compare-and-purchase to operate Coverage Cat's umbrella purchase workflow. Default to the consumer-prefill handoff when no operator bearer key is available in runtime, and only use the delegated flow when a real operator key is already present."
